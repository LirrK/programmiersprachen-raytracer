Fragen:

-> sphere.hpp
  ->  Warum müssen die override Funktionen in der hpp definiert werden?
      Wenn sie in der hpp deklariert und in der cpp definiert werden kompiliert es nicht.

-> Slides zu Klassenmechaniken?
-> wie soll print() funktionieren?